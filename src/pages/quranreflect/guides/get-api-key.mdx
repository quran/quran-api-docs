import { GuideLayout } from '@/components/guide-layout'

# How to get api key 

If you require an API key, please contact us via email at amer.abbas@gmail.com.

> To use QuranReflect.com's API, you'll need an API key as our API is not public.

export default ({ children }) => (
  <GuideLayout title="Getting started with Quran.com's OAuth2">
    {children}
  </GuideLayout>
)